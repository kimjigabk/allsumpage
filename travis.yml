language: node_js
node_js: node
cache:
  directories:
    - node_modules
services:
  - mongodb
deploy:
  provider: heroku
    api_key:
      secure: $HEROKU_API_KEY
  app: "allsum"
  on:
    branch: master
